import { DefaultTemplate } from "../components/DefaultTemplate"

export const HomePage = () => {
    return (
        <DefaultTemplate>
            <main className="flex-1">
            </main>
        </DefaultTemplate>
    )
}